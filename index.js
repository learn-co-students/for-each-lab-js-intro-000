function iterativeLog(array){array.forEach((element,index) => {console.log(`${index}: ${element}`)
})

  }
function iterate(callback){pets = ["dogs", "cats", "bats"];
callback(pets);
return pets; };
function doToArray(array,callback){array.forEach(callback);};
