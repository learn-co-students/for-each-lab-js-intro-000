function iterativeLog(array) {
  array.forEach((name, l) => {
    console.log(`${l}: {name}`)
  })
}


function iterate(callback) {
  const dogs = ["One", "Two", "Three"]

  cats.forEach(callback)

  return dogs
}

function doToArray(array, callback) {
  array.forEach(callback)
}

function doToArray(array, callback) {
  array.forEach(callback)
}
